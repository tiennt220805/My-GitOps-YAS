apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yas-staging-kafka
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/tiennt220805/My-GitOps-YAS.git
    targetRevision: HEAD
    path: infrastructure/base/kafka/kafka-cluster
    helm:
      valueFiles:
        - ../../../../environments/staging/values-shared.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: yas-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
