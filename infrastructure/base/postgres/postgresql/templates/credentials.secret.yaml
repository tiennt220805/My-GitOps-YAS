apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgresql.username }}.postgresql.credentials.postgresql.acid.zalan.do
  labels:
    application: spilo
    cluster-name: postgresql
    team: acid
type: Opaque
data:
  password: {{ .Values.postgresql.password | b64enc }}
  username: {{ .Values.postgresql.username | b64enc }}
