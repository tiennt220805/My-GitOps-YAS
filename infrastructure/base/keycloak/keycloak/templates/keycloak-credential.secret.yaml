apiVersion: v1
kind: Secret
metadata:
  name: keycloak-credentials
type: Opaque
data:
  username:
    {{ .Values.keycloak.bootstrapAdmin.username | b64enc }}
  password:
    {{ .Values.keycloak.bootstrapAdmin.password | b64enc }}
